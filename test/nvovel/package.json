{
  "name": "nvovel",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx samengine-build",
    "build": "npx samengine-build --release",
    "pub": "npx samengine-build --release --single-file"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "samengine": "^1.6.5",
    "samengine-build": "^1.6.5"
  }
}
