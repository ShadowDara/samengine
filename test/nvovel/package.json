{
  "name": "nvovel",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx samengine-build",
    "build": "npx samengine-build --release",
    "pub": "npx samengine-build --release --single-file"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "samengine": "file:../../packages/samengine",
    "samengine-build": "file:../../packages/samengine-build",
    "canvasinput-ts": "^1.0.0"
  }
}
